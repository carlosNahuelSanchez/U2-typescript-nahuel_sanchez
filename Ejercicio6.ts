// Se crea una funcion greet que recibe un parametro de tipo string y retorna un valor de tipo string
function greet (name: string): string {
    return `Hola, ${name}!`;
}

console.log(greet("Nahuel")); // Imprime: Hola, Nahuel!
