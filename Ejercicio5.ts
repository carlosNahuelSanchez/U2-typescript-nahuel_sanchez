// Se crea la funcion add que recibe dos parametros de tipo number y retorna un valor de tipo number
function add(a: number, b: number): number {
    return a + b;
}

// Se imprime el resultado de la funcion add con los valores 5 y 10
console.log(add(5,10))

